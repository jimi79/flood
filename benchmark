#!/bin/bash
a=0
mp=100
max=1000
for i in $(seq 1 $max);do
	b=$(./flood_create 19 19 7 | ./flood_solve -bx 9 -by 9 -mp $mp | wc -c)
	a=$(($a + $b))
	s=$(printf "5k %d %d / p" $a $i|dc)
	printf "avg = %s\n" $s
done
